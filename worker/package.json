{
  "name": "bowncr-waitlist-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Waitlist + anonymous checker counter for bowncr.app (Cloudflare Worker, KV, optional Resend confirmation).",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "lint": "node --check src/index.js",
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "npm run lint && npm test && npm run build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
